#!/bin/csh -f

#
# Configuration file
#

if ( ${?MGICONFIG} == 0 ) then
	setenv MGICONFIG /usr/local/mgi/live/mgiconfig
endif

source ${MGICONFIG}/master.config.csh

setenv NOMENLOADDIR     ${DATALOAD}/nomenload
setenv NOMENLOAD        ${NOMENLOADDIR}/nomenload.py
setenv MAPPINGLOADDIR   ${DATALOAD}/mappingload
setenv MAPPINGLOAD      ${MAPPINGLOADDIR}/mappingload.csh

