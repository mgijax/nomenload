#!/bin/csh -f

#
# Configuration file
#

if ( ${?MGICONFIG} == 0 ) then
	setenv MGICONFIG /usr/local/mgi/live/mgiconfig
endif

source ${MGICONFIG}/master.config.csh

setenv NOMENLOAD        ${DATALOAD}/nomenload/nomenload.py
setenv MAPPINGLOAD      ${DATALOAD}/mappingload/mappingload.csh
